# Local Docker Compose credentials. Copy this file to .env and adjust.
# These are LOCAL dev values for a throwaway container - do not reuse for any
# real/exposed database. The real .env is git-ignored.
POSTGRES_USER=snortforge
POSTGRES_PASSWORD=change_me_local_dev
POSTGRES_DB=snortforge
